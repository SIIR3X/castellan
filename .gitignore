# Secrets - NEVER commit

# Ansible Vault password / passphrase
.vault_pass
*.vault_pass
.vault-password*
vault-password*

# SSH private keys (must never enter the repository)
id_rsa
id_ed25519
id_ecdsa
*.pem
*.key
files/private_keys/

# Possible plaintext secret files (just in case)
secrets.yml
secrets.yaml
*.secret

# Ansible
*.retry
.ansible/
fact_cache/

# Local test harness secrets (disposable container keys) - never commit
test/secrets/

# Per-host configs are personal (real IPs, key paths) - never commit them.
# The tracked template is inventory/host_vars.example.yml; the dir is kept by
# its .gitkeep, and `harden init` writes inventory/host_vars/<host>.yml locally.
inventory/host_vars/*.yml

# Generated reports (folder tracked via .gitkeep, contents ignored)
reports/*
!reports/.gitkeep

# Python (ansible)
__pycache__/
*.py[cod]
.venv/
venv/

# System / editors
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/

# Build output
/dist/
