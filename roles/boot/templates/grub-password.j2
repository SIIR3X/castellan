#!/bin/sh
# Managed by Castellan - GRUB superuser + password (15.1). Do not edit.
# Protects editing boot entries and the GRUB shell; default menu entries stay
# --unrestricted so unattended reboots are not blocked.
cat <<'EOF'
set superusers="{{ boot_grub_superuser }}"
password_pbkdf2 {{ boot_grub_superuser }} {{ boot_grub_password_hash }}
EOF
