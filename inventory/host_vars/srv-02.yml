---
# Castellan per-host configuration for srv-02 (generated by ./harden init).

target_ip:        
connection_mode:  user_sudo
initial_user:     
initial_port:     

admin_user:        
admin_pubkey_file: 
sudo_mode:         nopasswd

ssh_port:          
ssh_allow_groups:  [ssh-users]
ufw_allowed_ports: []
f2b_ignoreip:      []
auto_reboot:       false

# >>> castellan: profile & measures (managed by ./harden configure) >>>
hardening_profile: standard
# <<< castellan <<<
