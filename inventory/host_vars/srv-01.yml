---
# Castellan per-host configuration for srv-01 (generated by ./harden init).

target_ip:        REPLACE_WITH_VM_IP
connection_mode:  root_password
initial_user:     root
initial_port:     22

admin_user:        lucas
admin_pubkey_file: /home/lucas/.ssh/id_ed25519.pub
sudo_mode:         nopasswd

ssh_port:          22
ssh_allow_groups:  [ssh-users]
ufw_allowed_ports: []
f2b_ignoreip:      []
auto_reboot:       false

# >>> castellan: profile & measures (managed by ./harden configure) >>>
hardening_profile: standard
# <<< castellan <<<
